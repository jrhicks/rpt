import AppDispatcher from '../dispatcher/AppDispatcher';
import { Store } from 'flux/utils';

class <%=ctx.camelizeName()%>Store extends Store {

  __onDispatch(action) {
    switch (action.type) {
      case '<%=ctx.camelizeName()%>/new':
        // TODO Modify data
        this.__emitChange();
        break;
      default:
        console.log(`Action identifier: ${action.type} not handled.`);
        break;
    }
  }
}

module.exports = new <%=ctx.camelizeName()%>Store(AppDispatcher);
